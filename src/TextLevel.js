// @flow
export type TextLevel = 'text:primary' | 'text:secondary' | 'text:hint' | 'text:dividers';

const PRIMARY: TextLevel = 'text:primary';
const SECONDARY: TextLevel = 'text:secondary';
const HINT: TextLevel = 'text:hint';
const DIVIDERS: TextLevel = 'text:dividers';

export {
  PRIMARY,
  SECONDARY,
  HINT,
  DIVIDERS,
};
